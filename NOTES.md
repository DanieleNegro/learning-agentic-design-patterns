# Notes

- Utente: computer scientist senior. Niente spiegazioni da zero su concetti di programmazione generale.
- Lingua: lezioni in italiano; terminologia tecnica sempre in inglese, mai tradotta (es. "workflow", "tool use", "hook", "skill", "subagent").
- Stack di riferimento per la mission: Claude Agent SDK / ecosistema Anthropic, non framework di terze parti.
- Glossario: creato in `reference/glossary.html` dopo il quiz 0001 superato con successo (2026-07-21). Contiene solo i termini verificati dal quiz (Workflow, Agent, Routing, Orchestrator-workers, Hook, MCP, Subagent). Aggiungere un termine nuovo solo dopo che è stato verificato in un esercizio, non solo "coperto" in una lezione.
- Convenzione lezioni dal 0002 in poi: un pattern di workflow per lezione (non più panoramica), sempre agganciato a un primitivo concreto — per i pattern nominati da Anthropic, il mapping naturale è sul Workflow tool di Claude Code stesso (`pipeline()` = prompt chaining, `parallel()` = parallelization, ecc.).
- Asset `diagrams.css` (SVG line-and-box in stile Anthropic) creato per riprodurre i diagrammi dell'articolo "Building effective agents" — riusabile per le lezioni sui prossimi pattern (routing, parallelization, orchestrator-workers, evaluator-optimizer).
- Lezione 0003 (routing, 2026-07-22): a differenza di prompt chaining/parallelization, routing NON ha un primitivo dedicato nel Workflow tool — è un agent() di classificazione seguito da un if/switch ordinario. Punto pedagogico esplicito nella lezione, non va presentato come lacuna. Prossimo pattern naturale: parallelization (già anticipato nel quiz 0003, domanda 5).
- Quiz 0003: Daniele ha sbagliato solo la domanda su "perché separare gli handler" (specializzazione vs costo) — vedi learning-record 0003-routing-mostly-mastered. Da ritestare con interleaving in una lezione futura, non ora.
- Lezione 0004 (parallelization, 2026-07-22): due varianti — sectioning (sotto-task diversi e indipendenti) e voting (stesso task ripetuto, maggioranza). Primitivo: parallel(), che è una barriera esplicita — contrasto pedagogico con pipeline() che non ha barriera tra stage. Prossimo pattern naturale: orchestrator-workers (già nel glossario dalla 0001, anticipato nel quiz 0004 domanda 5). Quiz 0004: 5/5, nessuna lacuna.
- Lezione 0005 (orchestrator-workers, 2026-07-22): perno concettuale = "il numero di sotto-task è deciso a runtime dall'orchestrator, non fissato nel codice" (contrasto diretto con parallelization). Agganciata esplicitamente a un esempio dal vivo della sessione stessa (Agent tool che delega a subagent). Nel Workflow tool si scrive come agent() che produce un piano + parallel() su plan.subtasks.map(...) invece di un array scritto a mano. Quiz 0005: 5/5, nessuna lacuna.
- Lezione 0006 (evaluator-optimizer, 2026-07-22): CHIUDE l'arco dei 5 pattern nominati da "Building effective agents" (chaining, routing, parallelization, orchestrator-workers, evaluator-optimizer). Perno concettuale = primo pattern con un ciclo esplicito; contrasto diretto con il gate di prompt chaining (ferma vs rimanda indietro con feedback). Nel Workflow tool si scrive come while attorno a due agent() (genera, valuta). Quiz include una domanda di ripasso (interleaving) che ri-testa la distinzione con prompt chaining.
- PROSSIMA DECISIONE (da confermare con Daniele): dopo la 0006, i 5 pattern di workflow sono coperti ma la mission ha altri assi ancora scoperti — Hook configurato, server MCP collegato, Skill custom costruita (vedi MISSION.md "Success looks like"). Proporre di spostare il focus lì, non continuare a numerare altri workflow pattern che non esistono nella tassonomia di Anthropic.
- Quiz 0006: 4/5. Sbagliata la domanda di ripasso: confonde "più chiamate LLM in sequenza" con "presenza di un ciclo" (vedi learning-record 0006-evaluator-optimizer-mostly-mastered). Da ritestare con interleaving in una lezione futura usando l'euristica esplicita "il flusso torna indietro a rigenerare uno step già eseguito?". Daniele ha poi confermato di aver capito la distinzione a voce (2026-07-22) — non ancora riverificata con un quiz, solo dichiarata.
- Decisione presa (2026-07-22): dopo la 0006, Daniele ha scelto l'asse "Skill custom" tra i tre rimasti nella mission (Skill, Hook, MCP). Prossime lezioni di questo filone: 0007 (anatomia di una skill: frontmatter minimo + progressive disclosure, con .claude/skills/teach/SKILL.md di questa stessa repo come esempio dal vivo), poi una lezione pratica dove Daniele scrive una skill minima da zero (ancora da fare). Fonte di riferimento: https://code.claude.com/docs/en/skills (fetchata per intero il 2026-07-22, non solo da conoscenza parametrica).
- Quiz 0007: 3/5. Sbagliate Q2 (livelli di progressive disclosure: sono 3 sequenziali, non 2 — i file di supporto NON caricano insieme al corpo all'invocazione) e Q4 (description, non name, è il campo che guida l'invocazione automatica di Claude). Vedi learning-record 0007-skill-anatomy-partial. Daniele ha riaperto il file (il quiz non persiste stato, nessun localStorage) e superato il quiz al secondo tentativo — vedi 0008-skill-anatomy-mastered.
- Lezione 0008 (build-a-skill, 2026-07-23): prima lezione PRATICA, non solo quiz — fa costruire a Daniele una skill personale reale (~/.claude/skills/review-checklist/) per una checklist di code review, sua scelta tra 3 opzioni proposte. Introduce dynamic context injection (!`comando`) e $ARGUMENTS. La checklist nello scheletro è volutamente segnaposto: Daniele deve sostituirla con i suoi criteri reali — questo è l'artefatto di mission "Skill custom costruita", non solo teoria.
- Quiz 0008: 5/5, nessuna lacuna. Daniele ha testato la skill review-checklist su un diff vero (2026-07-24) prima di rispondere al quiz — vedi learning-record 0009-build-a-skill-mastered. Asse "Skill custom" della mission ora coperto end-to-end. Restano scoperti solo Hook e MCP tra gli assi elencati in "Success looks like" — da confermare con Daniele quale affrontare per primo.
- Decisione presa (2026-07-24): Daniele ha scelto Hook come prossimo asse (tra Hook e MCP). Lezione 0009 (hook-anatomy): stesso schema anatomia-poi-pratica usato per Skill (0007→0008). Perno concettuale = "la Skill è una richiesta che il modello può violare, l'Hook è codice che gira sempre e può bloccare strutturalmente" — esplicitamente in contrasto con l'asse appena chiuso. Copre solo PreToolUse + handler di tipo command, e la semantica di exit code 0/2/altro (con nota che l'effetto di exit 2 dipende dall'evento). Fonte: https://code.claude.com/docs/en/hooks, fetchata per intero il 2026-07-24. Prossima lezione del filone: costruire e testare un hook vero (parallelo a 0008 per le skill).
- Quiz 0009: completato con successo, nessuna lacuna riportata — vedi learning-record 0010-hook-anatomy-mastered.
- Lezione 0010 (build-a-hook, 2026-07-27): pratica, parallela alla 0008 — fa costruire a Daniele un hook personale reale (~/.claude/hooks/block-force-push.sh, registrato in ~/.claude/settings.json) che blocca git push --force via PreToolUse su Bash. Esempio scelto non a caso: riflette il "Git Safety Protocol" già presente nel system prompt di questa sessione, per rendere tangibile la distinzione hook-vs-skill (regola chiesta al modello vs eseguita strutturalmente). Ancora da confermare con Daniele che l'abbia testato davvero (segnale che chiude l'asse Hook della mission, non solo il quiz).
